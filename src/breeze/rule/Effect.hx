package breeze.rule;

class Effect {
	macro public static function shadow(...exprs);

	macro public static function shadowColor(...exprs);

	macro public static function opacity(...exprs);

	macro public static function mixBlend(...exprs);

	macro public static function bgBlend(...exprs);
}
