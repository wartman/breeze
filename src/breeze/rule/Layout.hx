package breeze.rule;

class Layout {
	macro public static function container(...exprs);

	macro public static function columns(...exprs);

	macro public static function boxSizing(...exprs);

	macro public static function display(...exprs);

	macro public static function float(...exprs);

	macro public static function clear(...exprs);

	macro public static function isolate(...exprs);

	macro public static function overflow(...exprs);

	macro public static function overscroll(...exprs);

	macro public static function position(...exprs);

	macro public static function attach(...exprs);

	macro public static function visibility(...exprs);

	macro public static function layer(...exprs);
}
