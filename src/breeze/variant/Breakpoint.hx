package breeze.variant;

class Breakpoint {
	macro public static function viewport(size, ...exprs);

	macro public static function container(name, size, ...exprs);

	macro public static function markContainer(...exprs);
}
